name: fight_training_app
description: A simple martial arts training app with exercises and diet plan.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_tts: ^3.8.3
  audioplayers: ^5.2.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/diet/
    - assets/sounds/   # ← صحّحناها
